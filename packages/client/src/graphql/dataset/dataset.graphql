query getDatasets {
  getDatasets {
    _id
    name
    description
  }
}

query getDatasetsByProject($project: ID!) {
  getDatasetsByProject(project: $project) {
    _id
    name
    description
  }
}
