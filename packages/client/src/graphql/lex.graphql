query lexiconByKey($lexicon: String!, $key: String!) {
  lexiconByKey(lexicon: $lexicon, key: $key) {
  	key,
    primary,
    video,
    lexicon,
    associates,
    fields
  }
}

query lexFindAll {
  lexFindAll {
    _id,
    name,
    schema
  }
}
