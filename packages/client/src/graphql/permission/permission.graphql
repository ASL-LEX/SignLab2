query getProjectPermissions($project: ID!) {
  getProjectPermissions(project: $project) {
    user {
      id,
      projectId,
      fullname,
      username,
      email,
      role,
      createdAt,
      updatedAt,
      deletedAt
    },
    hasRole,
    editable,
    role
  }
}
