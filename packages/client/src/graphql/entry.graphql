query entryForDataset($dataset: ID!) {
  entryForDataset(dataset: $dataset) {
    _id,
    organization,
    entryID,
    contentType,
    dataset,
    creator,
    dateCreated,
    meta,
    signedUrl,
    signedUrlExpiration
  }
}
